---
layout: layouts/base.njk
---

{% set activities = collections.activity | createActivities %}

<h1>Runs:</h1>

<ul>
  {% for activity in activities %}
    <li>
      <a href="{{ activity.url }}">{{ activity.date.format('MMMM DD, YYYY') }}</a>
    </li>
  {% endfor %}
</ul>
